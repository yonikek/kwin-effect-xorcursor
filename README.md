Replace src/xorcursor.cpp with xorcursor-cursor-mask-fixed.cpp. The shader now discards pixels outside the cursor alpha mask and mixes the rendered scene with the color-managed inverted scene inside the cursor. This avoids the black rectangle caused by relying on alpha blending for the offscreen composite.
